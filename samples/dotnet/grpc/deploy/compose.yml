name: yaap-grpc
services:
  orchestrator:
    image: orchestrator
    ports:
      - "5173:5173"

  teamsagent:
    image: teamsagent
    environment:
      - URLS=http://teamsagent:5000
    depends_on:
      - orchestrator

networks:
  default:
